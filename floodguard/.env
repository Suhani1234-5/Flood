# -----------------------------------------------------------------------------
# SERVER-SIDE VARIABLES (Secret - Never expose to frontend)
# -----------------------------------------------------------------------------

# PostgreSQL Database Configuration (for Docker Compose)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password
POSTGRES_DB=mydb

# Database connection string (PostgreSQL)
# For Docker: Use 'db' as hostname, for local: Use 'localhost'
DATABASE_URL="postgresql://postgres:password@db:5432/mydb?schema=public"
# Local development (non-Docker): DATABASE_URL="postgresql://postgres:password@localhost:5432/mydb?schema=public"

# Redis connection string for caching
# For Docker: Use 'redis' as hostname, for local: Use 'localhost'
REDIS_URL="redis://redis:6379"
# Local development (non-Docker): REDIS_URL="redis://localhost:6379"

# Telegram Bot Token (for alerts)
TELEGRAM_BOT_TOKEN="your_bot_token_here"

# JWT Secret for authentication
JWT_SECRET="your_random_secret_string_here"

# -----------------------------------------------------------------------------
# CLIENT-SIDE VARIABLES (Safe - Prefixed with NEXT_PUBLIC_)
# -----------------------------------------------------------------------------

# Public API URL (e.g., for weather data or your own backend)
NEXT_PUBLIC_API_BASE_URL="http://localhost:3000/api"

# Public App Name
NEXT_PUBLIC_APP_NAME="FloodGuard"