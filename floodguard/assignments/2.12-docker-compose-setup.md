# Assignment 2.12 — Docker & Compose Setup for Local Development

**Assignment name:** Docker & Compose Setup for Local Development  
**Concept:** Brain 2.12  
**Deliverables:** Dockerfile, docker-compose.yml, README documentation.

---

## Git / Bash commands (for later reference)

### Branch and PR workflow
```bash
git checkout -b feature/docker-compose-setup
git add Dockerfile docker-compose.yml .dockerignore README.md .env.example
git commit -m "feat: add Docker and Docker Compose setup - Assignment 2.12"
git push -u origin feature/docker-compose-setup
# Create PR: feature/docker-compose-setup -> main
```

### Project commands
```bash
cp .env.example .env
docker compose pull
docker compose up --build
docker ps
```

---

## Quick reference: which PR / which commands

| Assignment / PR | Main commands |
|----------------|----------------|
| **2.12 Docker & Compose** | `docker compose pull`, `docker compose up --build`, `docker ps` |
| **2.13 PostgreSQL Schema Design** | `npm run db:generate`, `npx prisma migrate dev --name init_schema`, `npm run db:seed`, `npm run db:studio` |
